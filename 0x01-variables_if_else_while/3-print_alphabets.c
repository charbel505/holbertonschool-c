#include <stdio.h>
/* task 3 */

/**
 *main- print alphabets in lowercase then uppercase
 *Return: 0 (Success)
 *
 */
int main(void)
{
	char a;
	for (a = 'a'; a <= 'z'; a++)
		putchar(c);
	for (a = 'A'; a <= 'Z'; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
